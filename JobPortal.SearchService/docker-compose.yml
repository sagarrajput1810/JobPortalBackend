services:
  search-service:
    build: .
    container_name: search-standalone
    ports:
      - "5252:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ServiceBus__ConnectionString=YOUR_SERVICE_BUS_CONNECTION_STRING
